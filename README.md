# PR evidence

Local-browser screenshots produced for pull requests in a private repository,
hosted here so the review job and human reviewers can open them. GitHub has no
API for attaching an image to a pull-request description — the upload endpoint
takes a browser session only — so a CLI author cannot place them there directly.

Nothing here is product source. Every capture is of a local development server
with seeded fixture data and a throwaway local account.
